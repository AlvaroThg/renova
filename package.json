{
  "name": "renova",
  "version": "0.1.0",
  "private": true,
  "description": "RENOVA — Plataforma de valorización energética: landing narrativa + sistema de gestión del piloto de biogás",
  "scripts": {
    "dev": "pnpm --filter @renova/shared build && pnpm -r --parallel dev",
    "build": "pnpm --filter @renova/shared build && pnpm --filter @renova/api build && pnpm --filter @renova/web build",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "pnpm --filter @renova/api db:migrate",
    "db:seed": "pnpm --filter @renova/api db:seed",
    "db:studio": "pnpm --filter @renova/api db:studio",
    "setup": "pnpm install && pnpm db:up && pnpm --filter @renova/shared build && pnpm db:migrate && pnpm db:seed",
    "test": "pnpm --filter @renova/api test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
